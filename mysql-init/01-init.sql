-- SkyVault Database Initialization
-- This script will be run automatically by MySQL when the container starts

USE skyvault_dev;

-- All migrations and seeding are now applied by the application at runtime
-- This placeholder ensures the database exists and is ready

SELECT 'SkyVault database initialized' AS status;
