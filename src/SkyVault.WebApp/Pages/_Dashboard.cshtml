@model SkyVault.WebApp.Pages.IndexModel
@{
}
<link href="/css/dashboard.css" rel="stylesheet"/>
<div class="pagetopcontainer">
    <h2>
        Dashboard
    </h2>
    <button class="commonbtn">
        <a href="@Url.Page("CustomerProfile")" target="_self"><img class="plusicon" src="~/img/icons/plus_icon.png" alt=""/>Create Profile</a>
    </button>
</div>
<form>
    <div class="searcharea">
        <input class="form-control searchbox" id="searcher" name="searcher"
               hx-post="@Url.Page("Index", "Search")" hx-target="#search-results"
               hx-trigger="input changed delay:500ms, search"
               hx-headers='{"XSRF-TOKEN": "@Model.AntiForgeryToken"}'
               placeholder="Begin Typing To Search Profiles...">
    </div>
    <table class="table">
        <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
        </tr>
        </thead>
        <tbody id="search-results">
        </tbody>
    </table>
</form>